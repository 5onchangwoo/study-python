def gcd(n,m) :
    if n%m ==0 : return m
    else : return gcd(m,n%m)
def solution(arr):
    arr.sort()
    answer = arr[0]
    for i in range(1,len(arr)) :
        answer = arr[i]*answer / gcd(arr[i],answer)
    return answer
